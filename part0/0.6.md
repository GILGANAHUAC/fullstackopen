#EJERCICO 0.6  Nueva nota en diagrama de aplicación de una sola página.
```mermaid
sequenceDiagram
    participant browser
    participant server

    Note over browser: The user types a note and clicks Save

    Note over browser: The JavaScript code creates a new note, adds it to the local list, and renders the updated list on the screen

    browser->>server: POST [https://studies.cs.helsinki.fi/exampleapp/new_note_spa](https://studies.cs.helsinki.fi/exampleapp/new_note_spa)
    activate server
    Note over server: Server receives JSON payload: { "content": "nueva nota spa", "date": "2026-08-23" }
    server-->>browser: HTTP status code 201 Created
    deactivate server
